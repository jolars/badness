# Real-world corpus fetched by download.sh — not tracked (kept out of the repo to
# avoid bloat and licensing concerns). The committed baseline small.tex stays.
*.tex
!small.tex

# The multi-file project corpus for the folder benchmark (also fetched, untracked).
project/
