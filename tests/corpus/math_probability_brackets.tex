% Minimized from dalcde/cam-notes III_L/schramm-loewner_evolutions.tex
% (issue #55): the lone `]` belongs to `\gamma[`, so the outer `\P[` must
% stay an ordinary math atom instead of an unclosed optional argument.
\[
  0 < \P[\gamma[0, \infty) \cap A = \emptyset] < 1.
\]
